# 本書の執筆環境 {.unnumbered}

```{r}
#| include: false
knitr::opts_chunk$set(dev = "ragg_png")
```

**セッション情報**

```{r}
sessionInfo()
```

**インストール済みパッケージ**

```{r}
#| message: false
pacman::p_load(tidyverse)
installed.packages() %>%
  as_tibble() %>%
  select(Package, Version, Built) %>%
  print(n = Inf)
```